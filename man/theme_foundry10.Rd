% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_foundry10.R
\name{theme_foundry10}
\alias{theme_foundry10}
\title{Bold foundry10 ggplot2 theme}
\usage{
theme_foundry10(
  base_size = 12,
  base_family = .foundry10_font(),
  grid = TRUE,
  strip_color = "white"
)
}
\arguments{
\item{base_size}{Numeric. Base font size in pt (default 12). Use 14-16 for
slides, 10-11 for dense layouts.}

\item{base_family}{Character. Font family. Defaults to Helvetica Neue if
available, then Arial, then \code{"sans"}.}

\item{grid}{Logical. Show subtle horizontal grid lines (default \code{TRUE}).
Set \code{FALSE} for heatmaps and tile charts.}

\item{strip_color}{Character. Facet strip background: \code{"white"} (default),
\code{"orange"}, \code{"cyan"}, or \code{"black"}. Text is automatically white or black
for contrast.}
}
\value{
A ggplot2 theme object.
}
\description{
A clean, branded ggplot2 theme using the foundry10 visual identity:
Helvetica Neue typography, brand color accents, and generous whitespace
per the foundry10 Visual Style Guide.
}
\examples{
\dontrun{
library(ggplot2)
mpg6 <- mpg[mpg$class != "minivan", ]
ggplot(mpg6, aes(displ, hwy, color = class)) +
  geom_point(size = 2.5, alpha = 0.85) +
  scale_color_foundry10_d() +
  theme_foundry10() +
  labs(title    = "Engine displacement vs highway MPG",
       subtitle = "By vehicle class",
       x = "Displacement (L)", y = "Highway MPG")
}
}
